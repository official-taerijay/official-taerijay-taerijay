---
// 01-Header.astro
---
<header class="w-full px-4 md:px-12 py-4 border-b border-[#5B0E1B] bg-[#0A192F]/95 backdrop-blur sticky top-0 z-50">
	<!-- 데스크톱 & 모바일 공통 상단 프레임 -->
	<div class="flex items-center justify-between w-full relative">
		<!-- 좌측 햄버거 메뉴 버튼 -->
		<button id="menuTrigger" class="text-white hover:text-[#00F0FF] transition-colors p-2 z-10">
			<svg xmlns="http://www.w3.org/2000/svg" class="h-7 w-7 md:h-8 md:w-8" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
				<path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16" />
			</svg>
		</button>

		<!-- 중앙 로고 (모바일에서도 겹치지 않게 절대좌표 제거 및 반응형 마진 확보) -->
		<div class="flex flex-col items-center flex-1 text-center">
			<a href="/" class="font-spartan text-xl md:text-3xl font-black tracking-widest text-white leading-none uppercase select-none">TAERIJAY+</a>
		</div>

		<!-- 데스크톱 전용 우측 상단 로그인 버튼 (모바일에서는 아래 레이아웃으로 격리하여 감춤) -->
		<div class="hidden md:block z-10">
			<button id="desktopAuthBtn" class="px-5 py-2 border border-[#5B0E1B] rounded bg-[#5B0E1B]/30 hover:bg-[#5B0E1B]/60 text-white text-xs font-black uppercase font-spartan tracking-wider transition-all shadow-md">
				로그아웃 또는 회원가입
			</button>
		</div>
	</div>

	<!-- 모바일 전용 하단 분리 레이어 (TAERIJAY 타이틀 아래 우측 하단에 완벽 배치) -->
	<div class="flex justify-end w-full mt-2.5 md:hidden">
		<button id="mobileAuthBtn" class="px-3.5 py-1.5 border border-[#5B0E1B] rounded bg-[#5B0E1B]/30 hover:bg-[#5B0E1B]/60 text-white text-[11px] font-bold uppercase tracking-wider transition-all shadow-md font-noto">
			로그아웃 또는 회원가입
		</button>
	</div>
</header>

<!-- SIDEBAR DRAWER INTERFACE -->
<div id="sideDrawer" class="fixed inset-0 z-50 bg-black/80 backdrop-blur-sm hidden justify-start w-full transition-opacity duration-300">
	<div class="w-[320px] h-full bg-[#071120] border-r border-[#5B0E1B] p-6 flex flex-col gap-6 relative shadow-2xl overflow-y-auto font-noto">
		<div class="flex items-center justify-between border-b border-[#5B0E1B] pb-4">
			<span class="font-spartan text-lg font-black tracking-widest text-[#00F0FF] uppercase">CHANNELS</span>
			<button id="menuCloser" class="text-slate-400 hover:text-white text-3xl font-light leading-none">&times;</button>
		</div>
		
		<nav class="flex flex-col gap-3 font-bold text-sm">
			<a href="/" class="w-full text-left p-3.5 rounded border border-[#5B0E1B]/60 bg-[#0A192F] text-white hover:border-white transition-all font-playfair font-black tracking-wider uppercase text-base">0. TAERIJAY</a>
			<a href="/04-channels/01-entertainment" class="w-full text-left p-3.5 rounded border border-[#5B0E1B]/40 bg-[#0A192F] text-[#00F0FF] hover:border-[#00F0FF] transition-all text-base lowercase">1. taerijay+entertainment</a>
			<a href="/04-channels/02-protocol" class="w-full text-left p-3.5 rounded border border-[#5B0E1B]/40 bg-[#0A192F] text-[#FF6B00] hover:border-[#FF6B00] transition-all text-base lowercase">2. taerijay+protocol</a>
			<a href="/04-channels/03-mini" class="w-full text-left p-3.5 rounded border border-[#5B0E1B]/40 bg-[#0A192F] text-[#FBF9F4] hover:border-white transition-all text-base lowercase">3. taerijay+mini</a>
			<a href="/04-channels/04-daiso" class="w-full text-left p-3.5 rounded border border-[#5B0E1B]/40 bg-[#0A192F] text-[#FF2E2E] hover:border-[#FF2E2E] transition-all text-base lowercase">4. taerijay+daiso</a>
			<a href="/04-channels/05-oliveyoung" class="w-full text-left p-3.5 rounded border border-[#5B0E1B]/40 bg-[#0A192F] text-[#6F8B2E] hover:border-[#6F8B2E] transition-all text-base lowercase">5. taerijay+oliveyoung</a>
			<a href="/04-channels/06-emart" class="w-full text-left p-3.5 rounded border border-[#5B0E1B]/40 bg-[#0A192F] text-[#FFFB00] hover:border-[#FFFB00] transition-all text-base lowercase">6. taerijay+emart</a>
			<a href="/04-channels/07-convenience" class="w-full text-left p-3.5 rounded border border-[#5B0E1B]/40 bg-[#0A192F] text-[#A14D7C] hover:border-[#A14D7C] transition-all text-base lowercase">7. taerijay+convenience</a>
		</nav>
		<div class="mt-auto border-t border-[#5B0E1B] pt-4 text-center">
			<span class="text-xs text-slate-400 font-mono" id="drawerUserBadge">STATUS: GUEST_MODE</span>
		</div>
	</div>
</div>

<script is:inline>
	document.addEventListener('DOMContentLoaded', () => {
		const menuTrigger = document.getElementById('menuTrigger');
		const sideDrawer = document.getElementById('sideDrawer');
		const menuCloser = document.getElementById('menuCloser');
		
		const desktopBtn = document.getElementById('desktopAuthBtn');
		const mobileBtn = document.getElementById('mobileAuthBtn');
		const drawerUserBadge = document.getElementById('drawerUserBadge');

		if (menuTrigger) {
			menuTrigger.addEventListener('click', () => {
				sideDrawer.classList.remove('hidden');
				sideDrawer.classList.add('flex');
			});
		}
		const closeMenu = () => {
			sideDrawer.classList.add('hidden');
			sideDrawer.classList.remove('flex');
		};
		if (menuCloser) menuCloser.addEventListener('click', closeMenu);

		// 동기화 제어 루프 함수 (지시하신 로그인 / 로그아웃 또는 회원가입 텍스트 매핑 고정)
		const updateAuthButtons = (isAuthorized) => {
			const buttons = [desktopBtn, mobileBtn];
			buttons.forEach(btn => {
				if (!btn) return;
				if (isAuthorized) {
					btn.textContent = '로그인';
					btn.className = "px-4 py-1.5 md:py-2 border border-emerald-500 rounded bg-emerald-950/60 text-emerald-400 text-xs font-bold transition-all shadow-md font-noto";
				} else {
					btn.textContent = '로그아웃 또는 회원가입';
					btn.className = "px-3.5 py-1.5 md:py-2 border border-[#5B0E1B] rounded bg-[#5B0E1B]/30 hover:bg-[#5B0E1B]/60 text-white text-xs font-bold transition-all shadow-md font-noto";
				}
			});

			if (drawerUserBadge) {
				drawerUserBadge.textContent = isAuthorized ? 'STATUS: AUTHORIZED_VIP_USER' : 'STATUS: GUEST_MODE';
			}
		};

		// 초기 세션 상태 감지
		const currentSession = localStorage.getItem('taerijay_auth_state');
		updateAuthButtons(currentSession === 'authorized');

		// 클릭 리스너 공통 라우팅 바인딩
		const handleAuthAction = () => {
			const sessionCheck = localStorage.getItem('taerijay_auth_state');
			if (sessionCheck === 'authorized') {
				// 이미 로그인된 상태일 때 클릭하면 로그아웃 소각 처리함
				localStorage.removeItem('taerijay_auth_state');
				alert("정상적으로 로그아웃 소각 완료되었습니다.");
				window.location.href = "/";
			} else {
				// 로그아웃 상태일 때 클릭하면 인증 전용 허브 페이지로 라우팅함
				window.location.href = '/01-auth';
			}
		};

		if (desktopBtn) desktopBtn.addEventListener('click', handleAuthAction);
		if (mobileBtn) mobileBtn.addEventListener('click', handleAuthAction);
	});
</script>