---
import Layout from '../../layouts/Layout.astro';
import '../../styles/channel-page.css';

const tabs = [
  { slug: 'booking-tips', label: '예약·결제 서비스 안내', labelEn: 'Booking & Payment Tips', free: true },
  { slug: 'emergency-help', label: '응급·긴급 통역',       labelEn: 'Emergency Help',          free: true },
  { slug: '3hour',       label: '3시간 코스 (서울 12개)', labelEn: '3-Hour Course',           free: true },
  { slug: '1day',        label: '1일 코스',   labelEn: '1-Day Course', sub: '지역 내부' },
  { slug: '2day',        label: '2일 코스',   labelEn: '2-Day Course', sub: '타 지역 포함' },
  { slug: '3day',        label: '3일 코스',   labelEn: '3-Day Course', sub: '타 지역 포함' },
  { slug: 'region',      label: '지역별로',   labelEn: 'By Region', sub: '타 지역 포함' },
  { slug: 'recommend',   label: '추천코스',   labelEn: 'Recommended', sub: '지역 × 카테고리' },
  { slug: 'kdrama',      label: 'K-드라마 촬영지', labelEn: 'K-Drama Locations', sub: '단독 목록' },
  { slug: 'nightview',   label: '야경 명소 코스', labelEn: 'Night View Course', sub: '지역 × 카테고리' },
  { slug: 'photo',       label: '사진맛집 코스', labelEn: 'Photo Spot Course', sub: '지역 × 카테고리' },
  { slug: 'indoor',      label: '실내 코스 (우천 대비)', labelEn: 'Indoor Course', sub: '지역 × 카테고리' },
];
const color = '#e8e4dc';
---
<Layout title="taerijay+mini | TAERIJAY">
  <div class="channel-page" style={`--c:${color}`}>
    <div class="channel-topbar">
      <button type="button" class="back-btn" onclick="history.back()">← 뒤로가기</button>
    </div>

    <section class="channel-hero-banner" style={`--hero-img:url('/images/channels/mini.jpg');--c:${color}`}>
    </section>

    <section class="channel-hero">
      <span class="channel-badge channel-badge-lock notranslate" translate="no">무료 3개 + 유료 9개 · 총 12개 카테고리</span>
      <h1 class="channel-title">taerijay<span style={`color:${color}`}>+mini</span></h1>
      <p class="channel-sub">거점 도시별 최적 동선, 시간대별 큐레이션 코스. 100→1,000개 누적, 월 2회(10일·20일) 업데이트</p>
      <a href="/pricing" class="channel-subscribe-btn">구독하기 · Subscribe</a>
    </section>

    <section class="cat-grid-section">
      {tabs.map((tab) => (
        <a href={`/04-channels/mini/${tab.slug}`} class="cat-card">
          <div class="cat-card-art">
            <span class={`cat-card-tier notranslate ${tab.free ? 'is-free' : 'is-paid'}`} translate="no">{tab.free ? '무료' : '유료'}</span>
            <span class="cat-card-name notranslate" translate="no">{tab.label}</span>
            <span class="cat-card-name-en">{tab.labelEn}</span>
            {tab.sub && <span class="cat-card-sub notranslate" translate="no">{tab.sub}</span>}
          </div>
        </a>
      ))}
    </section>

    <section class="empty-note">
      <p>콘텐츠 준비 중입니다.</p>
      <p class="empty-note-en">Content coming soon.</p>
    </section>
  </div>
</Layout>